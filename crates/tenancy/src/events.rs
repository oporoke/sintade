//! Domain events published by `tenancy`. None yet: workspace creation today only happens inside
//! registration's transaction (see `TenancyService::create_personal_workspace`), and invites /
//! role changes arrive with V1's team features.
